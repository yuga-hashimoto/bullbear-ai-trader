# Core
numpy>=1.26
pandas>=2.1
pyyaml>=6.0
pyarrow>=14.0  # parquet storage (CSV fallback exists if absent)

# Data
yfinance>=0.2.40
requests>=2.31
pandas-market-calendars>=4.4

# Models
scikit-learn>=1.3
lightgbm>=4.0  # optional; sklearn models work without it

# Tests
pytest>=7.4

# Web dashboard (optional; read-only backtest viewer)
streamlit>=1.30

# moomoo OpenAPI (optional, only needed for the future live/moomoo data adapter)
# futu-api>=7.0.0
